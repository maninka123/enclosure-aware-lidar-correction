# Generated runs

Run `python -m dome_correction experiments --config configs/baseline.json --output outputs/run_name` from the repository root. Each new run contains figures, tables, synthetic point clouds and a manifest. Generated runs are ignored by Git; publish selected reviewed artifacts explicitly if needed.
